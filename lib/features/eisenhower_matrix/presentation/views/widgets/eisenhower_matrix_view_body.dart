import 'package:flutter/material.dart';

class EisenhowerMatrixViewBody extends StatelessWidget {
  const EisenhowerMatrixViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Eisenhower Matrix view body'),
    );
  }
}
