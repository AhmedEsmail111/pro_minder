import 'package:flutter/material.dart';

class HabitsViewBody extends StatelessWidget {
  const HabitsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'habits view body',
      ),
    );
  }
}
